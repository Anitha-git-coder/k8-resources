apiVersion: v1
kind: Pod
metadata:
  name: service-np-demo
  labels:   # these labels are used to registerthis pod with service 
    project: roboshop
    component: frontend
    environment: dev 
    purpose: node-port
spec:
  containers: 
  - name: nginx
    image: nginx