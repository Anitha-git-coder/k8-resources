apiVersion: v1
kind: pod
metadata:
  name: resources-demo
spec:
  containers: 
  - name: nginx
    image: nginx
    resources: 
      request: 
      memory: "68Mi"
      cpu: "100m" #1/10 of CPU
      # provide equal or more than request
    limits:  
      memory: "100Mi"
      cpu: "120m"